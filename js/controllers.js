'use strict';

/* Controllers */

var siteGenControllers = angular.module('siteGenControllers', []);

/*
  caputire the form input to start the serivce
*/

siteGenControllers.controller('siteGenCtrl', function ($scope) {
  

  
  
  
});
  
siteGenControllers.controller('iconController', function ($scope, iconData) {
  
  // controller for manage the icons
  
});

siteGenControllers.controller('PhotoController', function ($scope, photoData) {

  // controller for manage the photos from flickr

});

siteGenControllers.controller('ContentController', function ($scope, contentData) {

  // controller for manage the hipster lipsum content

});

siteGenControllers.controller('domainController', function ($scope, domainData) {

  // controller for manage the domain availability checker

});